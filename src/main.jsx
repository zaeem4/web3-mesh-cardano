import React from "react";
import ReactDOM from "react-dom/client";
import WebCardano from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WebCardano />
  </React.StrictMode>
);
